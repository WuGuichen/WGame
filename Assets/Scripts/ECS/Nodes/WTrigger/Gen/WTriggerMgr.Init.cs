namespace WGame.Trigger
{
    public partial class WTriggerMgr
    {
        public void Init()
        {
            // var eventType = new WtEventType();
            // var condition = WtCondition.Always;
            // var trigger = new WTrigger(eventType, condition);
            // trigger.Register();
            // trigger.Register();
            // trigger.onTrigger.Add(() => WLogger.Info("Hello O"));
            // WTriggerMgr.Inst.TriggerEvent(trigger.eventType);
            // trigger.onTrigger.Add(() => WLogger.Info("World !"));
            // WTriggerMgr.Inst.TriggerEvent(trigger.eventType);
        }
    }
}
